info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3001/auth/login
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
